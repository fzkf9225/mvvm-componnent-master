<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/cl_tab"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:orientation="vertical">

        <io.coderf.arklab.common.widget.customview.IndicatorTabLayout
            android:id="@+id/tabLayout"
            android:layout_width="0dp"
            android:layout_height="45dp"
            app:indicatorColor="@color/black"
            app:indicatorCornerRadius="8dp"
            app:indicatorHeight="4dp"
            app:indicatorWidth="12dp"
            app:indicatorWidthMode='fixed'
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toTopOf="@+id/viewpager"
            app:layout_constraintEnd_toEndOf="parent"
            app:selectedTextColor="@color/autoColor"
            app:tabTextSize="14sp"
            app:tabTextStyle="bold"
            app:unselectedTextColor="@color/dark_light" />

        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/viewpager"
            android:layout_width="0dp"
            app:layout_constraintTop_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintVertical_weight="1"
            android:layout_height="0dp" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
